name: Build and Push Docker Image to GHCR

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      actions: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true

      - name: Install Nix
        uses: nixbuild/nix-quick-install-action@v35

      - name: Restore and save Nix store
        uses: nix-community/cache-nix-action@v7
        with:
          primary-key: nix-${{ runner.os }}-${{ hashFiles('flake.nix', 'flake.lock') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-
          gc-max-store-size-linux: 1G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-
          purge-created: 0
          purge-last-accessed: P1DT12H
          purge-primary-key: never

      # nix-quick-install-action runs Nix single-user without a sandbox, so
      # dockerTools.pullImage's skopeo call runs directly as the runner user
      # against the real /run. Since /run isn't writable by non-root, skopeo
      # can't create /run/containers/<uid> for its auth.json and fails with
      # "permission denied". Pre-create it so the build can proceed.
      - name: Prepare writable container auth directory
        run: sudo install -d -m 0777 -o "$(id -u)" -g "$(id -g)" "/run/containers/$(id -u)"

      - name: Build Docker image with Nix
        run: |
          set -e
          nix build .#dockerImage
          nix-collect-garbage -d

          if [ ! -f result ]; then
            echo "Error: Nix build did not produce result"
            exit 1
          fi
          docker load < result

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Tag and push Docker image
        if: github.event_name != 'pull_request'
        run: |
          set -e

          # Get the image name from Nix build
          BUILT_IMAGE=$(docker images --format "{{.Repository}}:{{.Tag}}" --filter "dangling=false" | head -n 1)

          if [ -z "$BUILT_IMAGE" ]; then
            echo "Error: No Docker image found"
            exit 1
          fi

          echo "Built image: $BUILT_IMAGE"

          # Tag and push for each metadata tag
          echo "${{ steps.meta.outputs.tags }}" | while IFS= read -r tag; do
            if [ -n "$tag" ]; then
              echo "Tagging: $tag"
              docker tag "$BUILT_IMAGE" "$tag"
              echo "Pushing: $tag"
              docker push "$tag"
            fi
          done
